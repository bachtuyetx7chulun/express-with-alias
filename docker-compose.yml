version: '3.9'
services:
  redis:
    image: 'redis:alpine'
    container_name: 'redis'
    restart: always
    ports:
      - '6379:6379'
    env_file:
      - '.env'
    command: /bin/sh -c "redis-server --requirepass $$REDIS_HOST_PASSWORD"
